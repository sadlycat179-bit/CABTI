# CATBTI 封面猫咪探头：设计验收

## 对照目标

- 视觉真值：
  - `C:/Users/Qxi/AppData/Local/Temp/codex-clipboard-eba37e63-8138-4123-801e-f3837ceaa705.png`
  - `C:/Users/Qxi/AppData/Local/Temp/codex-clipboard-b0a37556-e3e0-4065-99b7-0ee5903961c4.png`
- 目标状态：猫咪从圆盘上半圆随机位置沿径向弹出，圆盘位于猫咪前景，只露耳朵与双眼。
- 目标交互：桌面鼠标悬停即收起；手机与键盘点击收起；阴影随弹出扩大、随收起缩小。

## 已实施修正

- 猫咪层级为 `z-index: 2`，圆盘层级为 `z-index: 4`，装饰层为 `z-index: 5`。
- 大佐与喇叭分别向圆心内缩图片高度的 `56%` 与 `57%`，鼻子、嘴、身体和爪子由圆盘遮挡。
- 圆盘半径继续按自身宽度的 `49.6%` 计算，避免 2° 旋转后的包围盒误差。
- 桌面悬停同时检查精细鼠标能力、`pointerType === "mouse"` 和指针是否位于真实圆边外侧。
- 手机、触控笔和键盘统一保留原生按钮 `click` 收起。
- PNG 轮廓阴影从弱小状态过渡到向圆外偏移的柔和大阴影，收起时同步缩小并淡出。

## 静态检查

- 字体与文案：未修改原封面标题、按钮和说明。
- 布局节奏：未修改圆盘尺寸、页面网格或标题区间距。
- 色彩：沿用现有棕色柔和阴影色，不新增高饱和装饰色。
- 图片质量：继续使用两张独立透明 PNG，无 SVG、占位图或 CSS 猫咪绘图。
- 交互源码：桌面 hover、移动端 click、键盘 click、页面切换清理和减少动态效果分支均保留。
- `node --check src/app.js`：通过。
- `git diff --check`：通过（仅 Git 换行符提示）。

## 阻塞项

- 当前浏览器安全策略禁止自动打开本地 `file:` 页面，因此无法获得修改后的浏览器截图。
- 因缺少浏览器渲染证据，无法按要求制作“用户截图 + 最终实现截图”的同画面对照，也无法实测桌面 hover 与手机 tap。
- 需要用户在已打开的本地页面手动刷新并提供一张新截图，之后才能完成最终视觉比较。

final result: blocked
